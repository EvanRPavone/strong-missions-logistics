<div class="container m-auto bg-white my-10 border border-grey-200 p-10">
    <h1 class="text-indigo-700">Active Sessions</h1>

    <% @active_sessions.each do |itinerary| %>
        <div class="flex flex-wrap items-center justify-start p-4 border-b border-grey-200 mb-6">
            <div class="flex-1 flex justify-between">
                <div>
                    <%= link_to itinerary.which_team, itinerary, class: "btn-link mr-4" %>
                </div>
                <div>
                    <%= readable_time(itinerary.arrival) %> &ndash;
                    <%= readable_time(itinerary.depart) %>
                </div>
            </div>

            <%= link_to "Remove Itinerary", itinerary, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-red ml-4" if admin?%>
        </div>
    <% end %>
</div>